{
  "mcpServers": {
    "ir-log-analyzer": {
      "command": "node",
      "args": ["${workspaceFolder}/mcp-server.js"],
      "env": {
        "API_URL": "http://localhost:3000"
      }
    }
  }
}
